let Koa,Router,bodyParser,mongoose,api;_861‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_861‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_861‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_861‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_861‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();








//비구조화 할당을 통해 process.env내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
  })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _861‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

//라우터 설정
router.use('/api', api.routes()); //api 라우트 적용

//라우터 적용 전에 bodyparser설정
app.use(bodyParser());

//app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

//port가 지정되어있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
  _861‍.g.console.log(`lsitening to port %d`, port);
});
